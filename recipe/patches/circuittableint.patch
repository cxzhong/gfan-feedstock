--- a/src/gfanlib_circuittableint.h	2024-08-13 00:00:15.000000000 +0800
+++ b/src/gfanlib_circuittableint.h	2026-03-23 18:15:56.115403092 +0800
@@ -25,6 +25,9 @@
   template<typename> struct MyMakeUnsigned;
   template <> struct MyMakeUnsigned<int>{typedef unsigned int type;};
   template <> struct MyMakeUnsigned<long int>{typedef unsigned long int type;};
+  // long int and long long int are distinct C++ types; on LP64 platforms such as
+  // macOS both are 64-bit, but int64_t may alias either one depending on the ABI.
+  template <> struct MyMakeUnsigned<long long int>{typedef unsigned long long int type;};
   template <> struct MyMakeUnsigned<__int128>{typedef unsigned __int128 type;};
 
   class MVMachineIntegerOverflow: public std::exception
@@ -92,6 +95,18 @@
 	return s.str();
 }
 
+// On platforms where int64_t aliases long long int (such as macOS with Clang),
+// long int and long long int are distinct types. A long int argument has no
+// exact toStr() match among the __int64_t/__int128_t overloads, causing ambiguity.
+#ifdef _64BITLONGINT
+static std::string toStr(long int b)
+{
+	std::stringstream s;
+	s<<b;
+	return s.str();
+}
+#endif
+
 class my256s{
 public:
 	__int128_t lo,hi;
@@ -214,6 +229,16 @@
 	return ((__int128_t)a)*((__int128_t)b);
 }
 
+// On platforms where int64_t aliases long long int (such as macOS), the word type
+// of CircuitTableInt64POD is long long int, which is distinct from long int even
+// when both are 64-bit. This overload resolves extMul calls with int64_t arguments.
+#ifdef _64BITLONGINT
+static __int128_t extMul(long long int a, long long int b)
+{
+	return ((__int128_t)a)*((__int128_t)b);
+}
+#endif
+
 static __uint128_t unsignedProd64(uint64_t x,uint64_t y)
 {
 	return __uint128_t(x)*__uint128_t(y);
@@ -591,8 +616,8 @@
 	    ret=min;
 
 	    {
-	    	auto ret2=CircuitTableIntPOD((s.v*denominatorDivisor.multiplicativeInverse)*boundA.v);
-	    	ret2=MIN(ret2.v,-ret2.v);
+	    	CircuitTableIntPOD ret2; ret2.v=(s.v*denominatorDivisor.multiplicativeInverse)*boundA.v;
+	    	ret2.v=MIN(ret2.v,-ret2.v);
 	    	return ret2;
 	    	if(ret.v!=ret2.v)
 	    	{
